import { NoAccessDirective } from './no-access.directive';

describe('NoAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new NoAccessDirective();
    expect(directive).toBeTruthy();
  });
});
