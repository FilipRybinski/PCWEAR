import { UserCirclePipe } from './user-circle.pipe';

describe('UserCirclePipe', () => {
  it('create an instance', () => {
    const pipe = new UserCirclePipe();
    expect(pipe).toBeTruthy();
  });
});
